package HW1;
public class Toolkit {
    // 100-90  --- A
    // 89-80   --- B
    // 79-70   --- C 
    // 69-60   --- D
    // 59-0    --- F 
    // Hint:  We are sending in an int (The int grade) and we want to return a string (The finalGrade)
    // Hint2: You will need to use some logic to determine the grade (IE < > <= >=)
    // Hint3: You will need to use some if and if else statements 
    public String gradeCalculator(int grade) {
        
        String finalGrade = "";

        
        return finalGrade;
    }
}
